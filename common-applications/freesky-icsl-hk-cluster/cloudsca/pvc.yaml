apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sca-volume
  namespace: sca
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: csi-nas