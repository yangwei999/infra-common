apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: log-data-volume
  namespace: majun-anti-poisoning
spec:
  storageClassName: "sas"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi